---
import ContactCTA from "../../components/ContactCTA.astro";
import Layout from "../../layouts/Layout.astro";
import ServiceCard from "../../components/ServiceCard.astro";
import { residentialServices } from "../../lib/services";

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  name: "Residential Cleaning Services in Singapore",
  itemListElement: residentialServices.map((service, index) => ({
    "@type": "ListItem",
    position: index + 1,
    name: service.name,
    url: new URL(`/residential/${service.slug}`, Astro.site ?? Astro.url.origin).toString()
  }))
};
---

<Layout
  title="Residential Cleaning Services Singapore | Home Cleaning Services"
  description="See residential cleaning services in Singapore, including spring cleaning, tenancy cleaning, post-renovation cleaning, floor care, fabric care, and disinfection."
  jsonLd={jsonLd}
>
  <section class="bg-white">
    <div class="container py-14 md:py-20">
      <h1 class="serif max-w-4xl text-6xl font-semibold leading-[0.9] text-ink md:text-8xl">
        Home cleaning services across Singapore
      </h1>
      <p class="mt-7 max-w-2xl text-lg leading-8 text-ink/68">
        From spring cleaning to move-out cleaning and fabric care, Home Cleaning Services helps households choose a scope
        that fits the home, timing, and condition.
      </p>
      <div class="mt-9 flex flex-wrap gap-3">
        <a class="focus-ring bg-clay px-6 py-4 text-sm font-bold uppercase tracking-[0.12em] text-white transition hover:bg-ink" href="/contact">
          Get a quote
        </a>
        <a class="focus-ring border border-border bg-white px-6 py-4 text-sm font-bold uppercase tracking-[0.12em] text-ink transition hover:border-clay hover:text-clay" href="/commercial">
          Commercial services
        </a>
      </div>
    </div>
  </section>

  <section class="container py-14" data-pagefind-body>
    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      {residentialServices.map((service) => <ServiceCard service={service} />)}
    </div>
  </section>

  <section class="bg-white py-14">
    <div class="container grid gap-10 md:grid-cols-[0.75fr_1.25fr]">
      <div>
        <h2 class="serif text-4xl font-semibold leading-tight text-ink md:text-5xl">Quoted around your home, not a generic package.</h2>
      </div>
      <div class="grid gap-5 md:grid-cols-3">
        {[
          ["Share the job", "Tell us your property type, rooms, service, and preferred date."],
          ["Confirm the scope", "We review condition, access, special requests, and any exclusions before quoting."],
          ["Prepare the clean", "The cleaner arrives with the agreed scope and priorities."]
        ].map(([title, body]) => (
          <div class="border border-border bg-linen p-6">
            <h3 class="font-bold text-ink">{title}</h3>
            <p class="mt-3 text-sm leading-6 text-ink/66">{body}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <ContactCTA />
</Layout>
