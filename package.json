{
  "name": "home-cleaning-services-directory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "test": "vitest run",
    "build": "astro build && node scripts/clean-build-output.mjs dist && pagefind --site dist && node scripts/clean-build-output.mjs dist",
    "build:astro": "astro build && node scripts/clean-build-output.mjs dist",
    "preview": "astro preview",
    "pages:deploy": "node scripts/clean-build-output.mjs dist && wrangler pages deploy dist"
  },
  "dependencies": {
    "@lucide/astro": "^1.45.0",
    "astro": "^7.3.2",
    "pagefind": "^1.3.0",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "autoprefixer": "^10.4.22",
    "typescript": "^5.8.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.53.0"
  }
}
